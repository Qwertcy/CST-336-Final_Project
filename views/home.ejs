<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>top100</title>
</head>
<body>
  <%- include("partials/nav.ejs") %>
  <%- include("partials/search.ejs") %>


  <h1>top100</h1>

  <% for (let artist of topX) { %>
    <div class="artist">
      <img id = "albumCoverImg" src="<%= artist.image %>" alt="Album Cover"><br>
      <a class= "titles"  href="/artistPage/<%= artist.name %>">
         #<%= artist.rank %> 
         <h3><%= artist.name %></h3>
      </a>
      <button type="button" 
        class="btn btn-primary open-modal-btn" 
        data-toggle="modal" 
        data-target="#rankingModal"
        data-name="<%= artist.name %>" 
        data-image="<%= artist.image %>">
        Rank Artist
      </button>
      <button type="button"
          class="btn btn-secondary open-info-modal-btn"
          data-toggle="modal"
          data-target="#infoModal"
          data-name="<%= artist.name %>"
          data-image="<%= artist.image %>"
          data-rank="<%= artist.rank %>"
          data-lastweek="<%= artist.last_week_rank %>"
          data-peak="<%= artist.peak_rank %>"
          data-weeks="<%= artist.weeks_on_chart %>">
        View Info
      </button>

    </div>
  <% } %>
      <!-- Rank Modal -->
<div class="modal fade" id="rankingModal" tabindex="-1" role="dialog" aria-labelledby="rankingModalTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      
      <div class="modal-header">
        <h5 class="modal-title" id="modalArtistName">Artist Name</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      
      <div class="modal-body">
        <div class="text-center">
          <img id="modalArtistImage" src="" alt="Artist Image" style="width:150px;height:150px;">
        </div>

        <div class="container mt-3">
          <div class="row">
            <div class="col">
              <p>Ranking 1:</p>
              <p>Ranking 2:</p>
              <p>Ranking 3:</p>
              <p>Ranking 4:</p>
              <p>Ranking 5:</p>
            </div>
            <div class="col">
              <input type="number" id="rank1" class="form-control mb-2">
              <input type="number" id="rank2" class="form-control mb-2">
              <input type="number" id="rank3" class="form-control mb-2">
              <input type="number" id="rank4" class="form-control mb-2">
              <input type="number" id="rank5" class="form-control mb-2">
            </div>
          </div>
        </div>

      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button id="submitRankings" type="button" class="btn btn-primary">Submit Rankings</button>
      </div>

    </div>
  </div>
</div>
<!-- Info Modal -->
<div class="modal fade" id="infoModal" tabindex="-1" role="dialog" aria-labelledby="infoModalTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">

      <div class="modal-header">
        <h5 class="modal-title" id="infoModalArtistName">Artist Info</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body text-center">
        <img id="infoModalArtistImage" src="" alt="Artist Image" style="width:150px;height:150px;" class="mb-3">
        <ul class="list-group">
          <li class="list-group-item"><strong>Current Rank:</strong> <span id="infoRank"></span></li>
          <li class="list-group-item"><strong>Last Week's Rank:</strong> <span id="infoLastWeek"></span></li>
          <li class="list-group-item"><strong>Peak Rank:</strong> <span id="infoPeak"></span></li>
          <li class="list-group-item"><strong>Weeks on Chart:</strong> <span id="infoWeeks"></span></li>
        </ul>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>

    </div>
  </div>
</div>
<!-- rank script -->
<script>
  $('#rankingModal').on('show.bs.modal', function (event) {
    var button = $(event.relatedTarget);
    var name = button.data('name');
    var image = button.data('image');

    var modal = $(this);
    modal.find('#modalArtistName').text(name);
    modal.find('#modalArtistImage').attr('src', image);
  });

  // submit button
  document.getElementById("submitRankings").addEventListener("click", () => {
    const ranks = [
      document.getElementById("rank1").value,
      document.getElementById("rank2").value,
      document.getElementById("rank3").value,
      document.getElementById("rank4").value,
      document.getElementById("rank5").value
    ];

    if (ranks.some(rank => rank === "")) {
      alert("Please fill in all ranking fields before submitting!");
      return;
    }

    console.log("Submitting rankings:", ranks);

    $('#rankingModal').modal('hide'); // close after submit
  });
</script>
<!-- info script -->
<script>
   $('#infoModal').on('show.bs.modal', function (event) {
    var button = $(event.relatedTarget);
    var name = button.data('name');
    var image = button.data('image');
    var rank = button.data('rank');
    var lastWeek = button.data('lastweek');
    var peak = button.data('peak');
    var weeks = button.data('weeks');

    var modal = $(this);
    modal.find('#infoModalArtistName').text(name);
    modal.find('#infoModalArtistImage').attr('src', image);
    modal.find('#infoRank').text(rank);
    modal.find('#infoLastWeek').text(lastWeek);
    modal.find('#infoPeak').text(peak);
    modal.find('#infoWeeks').text(weeks);
  });
</script>
<!-- SPACE -->
<!-- WORKING FORM CHANCGING -->

<!-- Rank Modal -->
<!-- Rank Modal -->
<!-- <div class="modal fade" id="rankingModal" tabindex="-1" role="dialog" aria-labelledby="rankingModalTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <form id="rankForm" method="POST" action="/submit-rankings">
        <div class="modal-header">
          <h5 class="modal-title" id="modalArtistName">Artist Name</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>

        <div class="modal-body">
          <div class="text-center mb-3">
            <img id="modalArtistImage" src="" alt="Artist Image" style="width:150px;height:150px;">
          </div>

          <div class="form-group">
            <label for="rank1">Ranking 1 (Number input, max 10):</label>
            <input type="number" id="rank1" name="rank1" class="form-control" max="10" required>
          </div>

          <div class="form-group">
            <label>Ranking 2 (Radio):</label><br>
            <div class="form-check form-check-inline">
              <input class="form-check-input" type="radio" name="rank2" value="1" required>
              <label class="form-check-label">1</label>
            </div>
            <div class="form-check form-check-inline">
              <input class="form-check-input" type="radio" name="rank2" value="2">
              <label class="form-check-label">2</label>
            </div>
            <div class="form-check form-check-inline">
              <input class="form-check-input" type="radio" name="rank2" value="3">
              <label class="form-check-label">3</label>
            </div>
          </div>

          <div class="form-group">
            <label for="rank3">Ranking 3 (Dropdown):</label>
            <select id="rank3" name="rank3" class="form-control" required>
              <option value="">-- Select --</option>
              <option value="A">A</option>
              <option value="B">B</option>
              <option value="C">C</option>
            </select>
          </div>

          <div class="form-group">
            <label>Ranking 4 (Checkboxes):</label><br>
            <div class="form-check">
              <input class="form-check-input" type="checkbox" name="rank4" value="Option 1" id="rank4a">
              <label class="form-check-label" for="rank4a">Option 1</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="checkbox" name="rank4" value="Option 2" id="rank4b">
              <label class="form-check-label" for="rank4b">Option 2</label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="checkbox" name="rank4" value="Option 3" id="rank4c">
              <label class="form-check-label" for="rank4c">Option 3</label>
            </div>
          </div>

          <div class="form-group">
            <label for="rank5">Ranking 5 (Text):</label>
            <input type="text" id="rank5" name="rank5" class="form-control" required>
          </div>

        </div>

        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          <button id="submitRankings" type="submit" class="btn btn-primary">Submit Rankings</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
  $('#rankingModal').on('show.bs.modal', function (event) {
    var button = $(event.relatedTarget);
    var name = button.data('name');
    var image = button.data('image');

    var modal = $(this);
    modal.find('#modalArtistName').text(name);
    modal.find('#modalArtistImage').attr('src', image);
  });

  document.getElementById("rankForm").addEventListener("submit", function (e) {
    const rank1 = document.getElementById("rank1").value;
    const rank2 = document.querySelector('input[name="rank2"]:checked');
    const rank3 = document.getElementById("rank3").value;
    const rank4 = Array.from(document.querySelectorAll('input[name="rank4"]:checked')).map(cb => cb.value);
    const rank5 = document.getElementById("rank5").value;

    if (!rank1 || !rank2 || !rank3 || rank4.length === 0 || !rank5) {
      e.preventDefault();
      alert("Please fill in all ranking fields.");
      return;
    }

    console.log("Form data will be submitted:", {
      rank1, rank2: rank2.value, rank3, rank4, rank5
    });
    
  });
</script> -->







</body>
</html>